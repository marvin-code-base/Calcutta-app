export default function App() {
  return (
    <div style={{ fontFamily: "sans-serif", padding: "2rem" }}>
      <h1>Calcutta App</h1>
      <p>
        Backend and scoring engine are wired up. UI (settings, entries, bids,
        dashboards) is still to be built.
      </p>
    </div>
  );
}
